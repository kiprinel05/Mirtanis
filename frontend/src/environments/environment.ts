export const environment = {
  production: false,
  // Public site is live in local dev. Flip to true to preview the "coming soon" gate.
  comingSoon: false,
  apiUrl: "http://localhost:8000/api/v1",
  uploadsUrl: "http://localhost:8000",
};
