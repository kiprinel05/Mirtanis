# Build context for the frontend image is the repo root (docker-compose.yml).
# Keep it lean: never ship installed deps, build output, or VCS metadata.
**/node_modules
**/dist
**/.angular
**/.git
**/.env
**/*.log

# Not needed by the frontend image (backend has its own build context).
backend/
deploy/
